node_modules

# superpowers scratch
.superpowers/

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# Generated by `wrangler types`
worker-configuration.d.ts

# OS
.DS_Store
Thumbs.db

# Env
# .env holds only PUBLIC_ build-time defaults (no secrets) and is committed so
# `$env/static/public` always resolves; the `!.env` overrides a global-gitignore
# `.env` rule. Real secrets go in .env.local (still ignored by `.env.*`).
!.env
.env.*
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
playwright-report
